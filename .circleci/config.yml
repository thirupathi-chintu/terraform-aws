version: 2.1
jobs:
  build:
    docker:
      - image: ubuntu
        working_directory: ~/example
    steps:
      - checkout
      - run: 
        name: terraform init
        command: |
          mkdir terra
          cd terra
          terraform init

      
          
